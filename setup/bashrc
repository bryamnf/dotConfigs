#If it's and interactive shell 
case $- in
    *i*)
      if [ -f "$HOME/dotconfigs/shell_config.sh" ]; then
        source "$HOME/dotconfigs/shell_config.sh" 
      fi
      ;;
    *) ;;
esac

######MODULES#######
source /usr/share/modules/init/bash
####################
