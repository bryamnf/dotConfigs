#If it's and interactive shell 
case $- in
    *i*)
      if [ -f "$HOME/.dotConfigs/ishell.sh" ]; then
        source "$HOME/.dotConfigs/ishell.sh" 
      fi
      ;;
    *) ;;
esac

######MODULES#######
source /usr/share/modules/init/bash
####################
